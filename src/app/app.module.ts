import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';
import { AppRoutingModule } from './app.routes'; // Corrija o caminho se necessário

@NgModule({
  imports: [
    BrowserModule,
    AppRoutingModule // Certifique-se de que este módulo é o correto
  ],
  providers: [],
})
export class AppModule { }
